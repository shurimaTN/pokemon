/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'blog.slashgear.dev', 'raw.githubusercontent.com', 'raw.githubusercontent.com'],
      },
}

module.exports = nextConfig
